@Library('jenkins-custom-libraries_v2') _


//see https://git.loyaltyplant.com/devops/jenkins-custom-library/blob/master/vars/mvnRelease.groovy
mvnRelease {
    javaBuildVersion = 21
   //Maven release command
   //mvnReleaseCmd   = 'mvn clean -X -B release:prepare release:perform'

   //Maven release rollback command
   //mvnRollbackCmd  = 'mvn release:rollback'

   //Slack channel for notifications
   //slackChannel    = '#server_builds'
}